export type Affiliation = {
  href: string;
  src: string;
};

export type ContactItem = {
  href: string;
  icon: string;
  title: string;
  desc: string;
};

export type NavItem = {
  label: string;
  href: string;
};

export type Testimonial = {
  date: string;
  desc: string;
  img: string;
  author: string;
  from: string;
};

export type Service = {
  icon: string;
  title: string;
  desc: string;
};
